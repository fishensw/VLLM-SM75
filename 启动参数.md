# Flash-Next TP8 建议配置

本文件已脱敏；示例目录为通用占位目录，需要替换为部署环境实际路径。

推荐组合：MTP5、4096预填充批次、2GiB KV/卡、256K总上下文、并发上限4。需使用本包补丁对应镜像；原版v0.1.6仅加载模板不足以复现。

```bash
export MODEL_ROOT=/srv/models
export CACHE_ROOT=/srv/vllm-next/cache
export IMAGE=vllm-sm75-next-ultra-0924:latest
export API_BIND=0.0.0.0
export API_PORT=18001
read -rsp "设置 API Key: " VLLM_API_KEY; echo
export VLLM_API_KEY
: "${VLLM_API_KEY:?API Key不能为空}"
mkdir -p "$CACHE_ROOT"

docker run -d --name vllm-sm75-next-ultra-0924 \
  --gpus all --restart no \
  --shm-size 64g --memory 240g --memory-swap 240g \
  --ulimit memlock=-1 --ulimit nofile=1048576:1048576 \
  -p "${API_BIND}:${API_PORT}:8000" \
  --mount "type=bind,src=$MODEL_ROOT,dst=/models,readonly" \
  --mount "type=bind,src=$CACHE_ROOT,dst=/cache" \
  -e VLLM_CACHE_ROOT=/cache/vllm \
  -e TRITON_CACHE_DIR=/cache/triton \
  --entrypoint python3 "$IMAGE" /opt/flashnext/serve.py \
  --api-key "$VLLM_API_KEY" \
  --speculative-config '{"method":"mtp","num_speculative_tokens":5,"model":"/models/fp8ple/runtime/mtp-int4-g32"}'
```

客户端 API Base URL 为 `http://<服务器IP>:18001/v1`，模型名称为 `Flash-Next-TP8`，API Key 填启动时设置的值。请在防火墙中仅允许需要访问的网络。仅需本机访问时将 `API_BIND` 改为 `127.0.0.1`。

命令显式指定MTP5，与镜像内置默认值一致。草稿目录对应宿主的 `$MODEL_ROOT/fp8ple/runtime/mtp-int4-g32`，需提前准备独立草稿权重。

等价脚本：设置环境变量后运行`bash scripts/run-next-api.sh`。镜像内`/opt/flashnext/selected-config.json`保存完整参数，对应包内`config/selected-config.json`。

### 面板启动

需要 Ultra 面板时，使用以下方式启动，替代上面的纯 API 命令。模型和缓存目录沿用上方设置：

```bash
export ULTRA_DATA_ROOT=/srv/vllm-next/ultra
export CONSOLE_PORT=1615
bash scripts/run-next.sh
```

`ULTRA_DATA_ROOT` 使用独立的 Next 面板数据目录。脚本同时映射 `1615:1615` 和 `18001:8000`，自动启动面板，并导入、启动 **TP8＋MTP5＋256K＋并发4** 推荐配置。后续重启保留面板中的修改。两种启动方式选一种，同一组GPU只运行一个实例。

浏览器访问 `http://<服务器IP>:1615`。在宿主机读取登录 token：

```bash
docker exec vllm-sm75-next-ultra-0924 node /opt/sm75-workbench/console/auth-cli.mjs show
```

面板模式的推理 API Key 在面板中查看或修改，与面板登录 token 不同，无需设置 `VLLM_API_KEY`。API 地址仍为 `http://<服务器IP>:18001/v1`；模型加载进度可在面板的引擎日志中查看。

| 参数 | 取值 |
|---|---|
| model | /models/fp8ple |
| dtype / hf-overrides | float16 / dtype=float16 |
| tensor-parallel-size / enable-expert-parallel | 8 / 开启 |
| max-model-len / max-num-seqs | 262144 / 4 |
| max-num-batched-tokens | 4096 |
| kv-cache-dtype / kv-cache-memory-bytes | fp8_e4m3 / 2147483648 |
| gpu-memory-utilization | 0.90（显式KV bytes优先控制KV分配） |
| block-size / enable-prefix-caching | 32 / 开启，实际block会按模型布局调整 |
| engram-config | cpu_offload=true，保留PLE投影驻GPU |
| gdn-prefill-backend | flashqla_sm75 |
| speculative-config | method=mtp，num_speculative_tokens=5，model=/models/fp8ple/runtime/mtp-int4-g32 |
| compilation-config | mode=0，cudagraph_mode=PIECEWISE，capture_sizes=[1,2,4,6,12,24] |
| 服务端口 | 容器8000，宿主默认0.0.0.0:18001，可通过服务器IP访问 |

镜像环境：`OMP_NUM_THREADS=1`、`VLLM_SM75_QWEN38_HC_GEMV=1`、`VLLM_FLASHINFER_WORKSPACE_BUFFER_SIZE=134217728`。

内存设置来自约251GiB RAM的八卡测试主机；240g是上限而非固定预占。内存包含PLE表、文件/共享内存及推理运行时。需要为操作系统和其它服务留出空间。

上方 Docker 命令启动纯推理 API；需要面板时使用“面板启动”中的脚本。

256K表示输入与输出总长度；并发4用于混合长度，不保证四路各满256K。当前prefill不退化目标尚未达成，具体业务仍需验证。
