# v0.1.6 上传前脱敏审核

审核范围为发布源码的 Git 跟踪文件和重新生成的验收证据包；本次 GitHub 发布提供源码与验收资料，不包含公共容器镜像。本地正式 `vllm-sm75:v0.1.6-ultra` 镜像及四份指定的 Unraid 模板已安装，Ultra 管理容器已恢复运行，其余三个容器保持未启动。

- 已删除 1M 推理容量测试结果、截图和原始证据，不在发布包中保留 1M 推理通过声明。YaRN 1M 配置能力和参数示例保留；它们不表示 1M 推理已经验收。
- 已清除源码中测试机地址、用户主目录及面板示例中的宿主私有路径。文档中的 API 密钥都是明确的替换占位符；`192.168.1.x` 仅用于网段策略单元测试和示例。
- 源码扫描未发现真实凭据前缀、Bearer 令牌、私钥块或可疑的已赋值密钥。证据包逐文件排除了密钥、账户、对话和缓存文件，并记录 SHA256。
- 发布应使用从公开 v0.1.5 基线建立的单提交净化分支；包含早期原始测试记录的开发分支和旧打包文件不得推送。发布前再次核对分支历史与归档文件清单。

四张 T10 的完整镜像验收记录仍明确列出标准版 128K decode 回归和跨镜像 token 不等价；本地正式镜像在已测 Ultra 镜像上补入静态示例脱敏、同一页面整合 DSH、单侧栏与统一设置、中文目录工具和 Watcher 原生统计；推理层未改动。本次界面补丁通过 Node 回归及无 GPU 镜像真实浏览器验收，未单独重复 GPU 性能测试。其他硬件和模型需单独验收。

本轮统一界面补丁补充核对了单行顶栏、横向设置、实际镜像首开、全局字号及设置持久化。新增结构化证据仅包含合成指标和验证结果，界面截图来自隔离环境；正式账户、对话、密钥、私有宿主路径及回滚快照不进入发布包。发布源码、验收包完成后再次扫描归档内容。

## 本轮源码整理

- 控制台版本元数据统一为 `0.1.6`；中文和英文 README 保留主要更新、性能及构建入口，移除界面布局与小修复细节；详细行为保留在 Ultra 指南和验收文档。
- 清理当前指南中的旧字号范围与“验收待完成”表述；历史性能、截图和验收证据继续保留原适用范围。
- Git、Docker 上下文和 Ultra 候选打包排除本地环境文件及生成缓存；新增合成私有文件打包检查，确认其不会进入产物。
- 本轮构建入口及打包检查 21 项通过；控制台 203 项检查中 196 项通过、7 项环境相关跳过。没有重复 GPU 或长上下文测试。
- GitHub 源码 ZIP 与 TAR 只包含净化发布提交的跟踪文件，不包含 `.git` 历史、正式配置、账户、对话、测试机部署脚本或回滚快照。实际镜像运行与浏览器验收仍按原记录解释；本次源码整理没有重建正式镜像。
