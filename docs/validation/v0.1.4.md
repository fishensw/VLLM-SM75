# v0.1.4 验证记录

日期：2026-09-10。PR #7 测试提交 `8792b87a99982636a7f7b76d336f9c002a98801c`，加四项本地兼容修补。未将本地整合稿冒充该实测版本。

## PR 兼容修补版本

原 PR 构建失败；修正测试协议导入、FA2 布局接口及草稿布局同步后，完整多阶段构建内 44 项测试通过。首次 GPU 启动另发现 AutoWeightsLoader 接口错误，补上第四项修补后成功启动并完成以下测试。44 项测试不等于四项修补后的全部 GPU 功能验收。

| 输入 | v0.1.3 prefill tok/s | PR prefill tok/s | 变化 | v0.1.3 decode tok/s | PR decode tok/s |
|---|---:|---:|---:|---:|---:|
| 1K | 1171.66 | 1216.17 | +3.80% | 178.79 | 159.74 |
| 4K | 1211.01 | 1274.75 | +5.26% | 143.26 | 183.40 |
| 8K | 1210.61 | 1272.23 | +5.09% | 182.20 | 182.36 |
| 16K | 1196.49 | 1254.19 | +4.82% | 180.47 | 180.60 |
| 32K | 1152.10 | 1205.85 | +4.66% | 178.42 | 179.12 |
| 64K | 1072.64 | 1118.39 | +4.27% | 173.87 | 175.15 |
| 128K | — | 978.19 | — | — | 166.39 |

4× T10 16 GiB，TP4，CPU ondemand；DFlash draft7、seq4、batch8192、FP8 e4m3 KV、CPU KV 8 GiB。FP8 all-reduce 四卡实际选择 P2P，较小消息回退 NCCL；本次没有单变量关闭 Firefly 的对照，不能把全部提升严格归因于单一内核。

固定相同输入 token，流式 completions，temperature=0、ignore_eos=true、输出512 tokens，独立 cache_salt。prefill=输入tokens/TTFT；decode=(输出tokens−1)/(最后与首个文本事件时间差)。旧版取已保存三轮中位数，未重跑；PR 每长度一轮。32-token 预热不计入计时，预热中出现 DFlash/采样 JIT。七组测量 cached_tokens=0、抢占=0。

8–64K 输出与旧版一致，decode 基本持平；1K/4K 输出和草稿接受率改变，不能据此宣称 decode 普遍提速。128K 没有旧版可比记录。额外算术请求 17×23 返回391，仅是冒烟检查，不是模型质量评估。合成输入的高接受率不代表日常聊天。

## 移入整合稿的既有修复及独立证据

以下来自此前独立 0.29 候选镜像，并非上述 PR 测试镜像，也不是最终 v0.1.4 整合验证：

- CPU KV 恢复：6090/24554 输入分别命中4992/23296 tokens，耗时5.41→1.15秒、21.52→1.73秒，输出正确一致，记录实际 CPU→GPU 加载。
- 60秒 exit：API在线，四卡12次稳定样本均P8、3MiB/卡，平均总功耗55.22W；并发唤醒取消一个请求，另一个成功，耗时137.14秒。
- 30分钟启动、60秒启动、exit唤醒均12次 AOT 命中、零计算图重新编译。
- 后续 JIT 修补镜像三次实际请求 TTFT 0.241/0.380/1.115秒，均无 JIT 警告；未再重跑完整休眠周期。

## 整合镜像部署检查与剩余验证范围

完整构建与镜像安装哈希；FP8 DFlash2 正常输出、CPU KV 实际恢复；60秒 exit 后P8与并发唤醒；三组件缓存复用及首请求JIT日志。保留30分钟正式配置，无须重跑旧版基线。AWQ/MTP与CPU/reload若未补测，应继续明确标为未验收，不延伸当前结论。

## AWQ 修复、优化与实测

保留AWQ dtype/布局适配和DFlash2加载修补，合入Firefly INT4 prefill、FP8 all-reduce、CPU KV分组及首次请求预热修复；启动参数沿用AWQ独立配置。PR兼容镜像已完成1–128K测试：32K prefill/decode为1433.67/215.57 tok/s，128K为1124.88/198.40 tok/s。后续整合修复尚待整包验收，不能据此宣称CPU KV恢复或AWQ休眠已验证。

[AWQ推荐配置](../recommended-awq-dflash2.md) · [完整AWQ数据](v0.1.4-awq.md)

## v0.1.4 整合镜像部署结果

2026-09-10，完整构建48项测试通过。镜像ID `sha256:146639e683c2772e4e003eca4b281aa03a7eacd28b847d31843cf5b9f22463a1`；27份主overlay和8份speculative源码与构建源码一致。AWQ DFlash2按原参数启动，health/monitor/metrics均200；算术请求返回391，24输入/4输出tokens、总耗时0.488秒。12次AOT命中、四卡输入/采样预热完成、首次请求无JIT警告；草稿组标记仅14，30分钟exit配置保留。没有在本轮重跑CPU KV实际恢复或完整休眠/P8周期。发布前后仅有文档整理，运行代码与此构建一致。
