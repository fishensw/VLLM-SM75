# vLLM-SM75 v0.1.3 构建验证

## 初版历史构建结果

- 基础镜像：官方 `vllm/vllm-openai:v0.28.0-cu129`（固定 digest）。
- 构建标签：`local/vllm-sm75:v0.1.3`。
- 镜像 ID：`sha256:e5e03506b4ee7335a42c1897d0a75a5134118145e1854067c03e82ddbf48d011`。
- 镜像大小：约 31.19 GB。
- 运行时版本：vLLM 0.28.0、PyTorch 2.13.0+cu129、FlashInfer 0.6.18。

## 构建内检查

- SM75 backend selector、GDN selector、参数脱敏、dtype 覆盖和 auto-sleep 测试：44 项全部通过。
- `AutoSleepConfig(1800, "exit")` 可正常加载。
- 未执行模型服务启动；本次只验收镜像构建、适配文件和启动参数解析。

## PR #6 本地实测

- FP8 MTP5、TP4、exit 模式自动卸载后，四卡显存约 3 MiB/卡，GPU 进入 P8，平均功耗约 10–15 W/卡。
- 透明唤醒、并发取消及剩余请求恢复通过。
- `docker/run.sh` 默认空闲 30 分钟；设置 `AUTO_SLEEP_IDLE_TIMEOUT=0` 可关闭自动休眠。

## 2026-09-08 同版本缓存修复验证

修复镜像 `sha256:d70591b3d5e89c28a63ca3b7e1ffa08b5dfc466ffce1fd6b81381dce78e8827d`，正式标签仍是 `vllm-sm75:v0.1.3`。完整多阶段重建通过，镜像中的三份缓存修复源码哈希与构建源码一致；构建内 44 项测试、新增 6 项缓存回归测试通过。

FP8 DFlash2：已有缓存启动、60 秒 exit 唤醒、30 分钟配置启动，均 12 次 AOT 命中、0 次重新编译且缓存键相同。60 秒休眠时四卡 P8、3 MiB/卡、9.97–15.31 W/卡，API 在线；并发唤醒短请求成功，完整耗时约 135.7 秒。

[完整推荐参数、测量口径与边界](../recommended-fp8-dflash2.md)。后续准备的启动脚本目录/模式选项变更另经 11 项 Bash 参数/挂载检查，文档中的完整推荐命令也与正式容器推理参数逐项比对通过。脚本检查不等于重新进行所有模型 GPU 验收。

新增 6 项缓存回归可以在修复镜像内单独复现（不需要 GPU、不访问网络）：

```bash
for test in tests/test_sleep_compile_cache.py \
  tests/config/test_compile_cache_runtime_state.py \
  tests/v1/engine/test_respawn_cache_config.py; do
  docker run --rm --network none --entrypoint python3 \
    --volume "$PWD:/test:ro" vllm-sm75:v0.1.3 "/test/$test"
done
bash tests/test_v012_launch.sh
bash tests/test_sleep_launch.sh
```
