# vLLM-SM75 v0.1.3 构建验证

## 构建结果

- 基础镜像：官方 `vllm/vllm-openai:v0.28.0-cu129`（固定 digest）。
- 构建标签：`local/vllm-sm75:v0.1.3`。
- 镜像 ID：`sha256:e5e03506b4ee7335a42c1897d0a75a5134118145e1854067c03e82ddbf48d011`。
- 镜像大小：约 31.19 GB。
- 运行时版本：vLLM 0.28.0、PyTorch 2.13.0+cu129、FlashInfer 0.6.18。

## 构建内检查

- SM75 backend selector、GDN selector、参数脱敏、dtype 覆盖和 auto-sleep 测试：44 项全部通过。
- `AutoSleepConfig(1800, "exit")` 可正常加载。
- 未执行模型服务启动；本次只验收镜像构建、适配文件和启动参数解析。

## PR #6 本地实测

- FP8 MTP5、TP4、exit 模式自动卸载后，四卡显存约 3 MiB/卡，GPU 进入 P8，平均功耗约 10–15 W/卡。
- 透明唤醒、并发取消及剩余请求恢复通过。
- `docker/run-v0.1.3.sh` 默认空闲 30 分钟；设置 `AUTO_SLEEP_IDLE_TIMEOUT=0` 可关闭自动休眠。
