# 工作台默认参数显示修复（历史验证）

后续已[区分服务默认与模型推荐](2026-09-18-model-recommendations.md)，本记录中的数值是服务默认值，不是模型推荐值。

工作台原先只显示已保存的覆盖值，未设置的字段统一显示“模型默认”，无法看到具体数值。现在按所选本地模型对应的启动配置解析默认值，并以“默认 数值”的占位文本显示；未填写的字段仍不随请求提交，保存或恢复默认不改变继承行为。

尊重 `--generation-config vllm`、模型生成配置、启动覆盖配置和工作台输出预算。当前已核对配置为温度 1、Top P 1、Top K 0、Min P 0、重复惩罚 1、最大输出预算 32768。输出预算随剩余上下文调整；无法确认的外部模型显示“默认值未提供”。Top K 为 0 或 -1 表示不限制，两者均可保存。

- 源码：`d9c8f190c75e322d505f6166e617462f99d215b7`。
- 独立 UI 候选：`local/vllm-sm75:v0.1.5-ultra-sampling-review`。
- 镜像 ID：`sha256:34793e32e43a6ddc34630659e81efa59eba0d55131d1bc6649c7121fa9a1228a`。
- 基座：已完整构建验收的 ultra `sha256:54b5f40e12618348300d838a772cab569db355bc59a3ad68e223dd691d64eaa2`。
- 镜像内 Linux 测试 56/56；Windows 54 通过、2 个平台测试跳过。
- 独立容器 HTTP 验证通过：读取默认值、保存显式 0 与自定义值、恢复空覆盖；浏览器预览确认六项默认值可见。

本次为 UI/参数显示及校验修复，使用独立的增量候选，没有重新执行完整 GPU 长上下文验收。推理源码与构建配方不变，之前的 22 项结果仍明确归属于原完整镜像。生产模型没有停止或替换，候选等待确认，未推送发布。
