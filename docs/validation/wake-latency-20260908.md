# PR exit 唤醒耗时差异排查

2026-09-07 报告：PR `558ff3a` 增量镜像 `29fa66eb...`，第一次引擎重建约 172.1 秒、TTFT 173.132 秒。

2026-09-08：正式 v0.1.3 镜像 `e5e03506...`，首次重建日志 327.5 秒，请求总耗时 331.044 秒；第二轮并发请求 335.076/337.443 秒。不能用 TTFT/总耗时口径差异解释翻倍。

| 当前首次重建阶段 | 本地时间 | 阶段耗时 |
|---|---|---:|
| 请求触发重建 → EngineCore 初始化 | 11:21:15 → 11:21:52 | 37 秒 |
| EngineCore → TP0 分布式初始化 | 11:21:52 → 11:22:26 | 34 秒 |
| TP0 → TP3 分布式初始化 | 11:22:26 → 11:23:40 | 74 秒 |
| TP3 初始化 → 开始模型加载 | 11:23:40 → 11:24:02 | 22 秒 |
| 模型加载 | 11:24:02 → 11:25:03 | 61 秒（内部加载计时 58.34 秒） |
| 加载完成 → 引擎预热完成 | 11:25:03 → 11:26:17 | 74 秒（实际编译约 9.61 秒） |
| 预热完成 → 重建就绪 | 11:26:17 → 11:26:42 | 25 秒 |
| 请求返回 | 约 11:26:46 | 约 4 秒 |

只读检查发现 CPU driver=intel_cpufreq、governor=powersave，min=1.2 GHz、max=4.5 GHz。20 个逻辑核心在 Worker 各自约 90% CPU 占用时仍全部约 1.2 GHz。此驱动的 generic powersave 请求最低频率，是当前启动和预热性能瓶颈的直接证据；旧报告没有 CPU 策略记录，尚不能证明它解释了全部差值。

另外，旧测试脚本使用 host 网络/18006，新测试保留正式 br0 静态地址/8000；基础镜像 ID 也不同。尚未进行配对对照，不将网络差异判为根因。

用户授权临时切换 schedutil，完成对照后恢复。2026-09-08 12:06:43 对照通过，脚本随后恢复原 powersave 策略，五配置 P8 验收继续。

| 同镜像、同参数、同脚本的并发唤醒 | 算术请求总耗时 | OK 请求总耗时 |
|---|---:|---:|
| powersave（负载下约 1.2 GHz） | 335.076 秒 | 337.443 秒 |
| 临时 schedutil（负载核心约 3–4.5 GHz） | 169.493 秒 | 169.421 秒 |

算术请求耗时降低 49.4%。两组均使用原 v0.1.3 镜像、原 FP8 MTP5 参数、同一个容器和 verify-mode.cjs；顺序运行而非随机重复实验，缓存可能存在顺序影响。schedutil 组模型加载仍约 53 秒，明显改善出现在进程初始化和预热阶段。当前低频 CPU 策略是本轮变慢的主要原因；旧报告未记录 CPU 频率，不能反推旧宿主机的策略。

schedutil 对照休眠后连续三次四卡均为 P8、每卡 3 MiB，功耗 9.87–12.32 W；并发唤醒输出 43/OK，随后请求 0.877/0.728 秒，输出一致。原始证据在宿主机 disk-sleep-20260908 下：exit-fp8-mtp5-powersave-result.json、exit-fp8-mtp5-schedutil-result.json、exit-fp8-mtp5-schedutil-test.txt、cpu-policy-before.tsv。没有永久修改 CPU 策略。

12:12:11 已逐一验证全部 20 个 policy 恢复为 powersave。随后用户自行调整策略并要求继续测试；12:13:54 核对全部 20 个 policy 为 ondemand，负载核心约 3–4.5 GHz，保留用户设置。AWQ MTP5 首次启动跨越这次变更，不能作为单一 governor 的启动耗时；此后的唤醒与剩余配置按 ondemand 记录。
