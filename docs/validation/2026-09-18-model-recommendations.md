# 模型推荐与服务默认参数分离

此前把服务默认采样参数标为“模型默认”不准确。本次将服务继承值明确标为“服务默认”，并按用户提供的 Qwen3.8-27B 模型说明增加两套推荐。

| 模式 | temperature | top_p | top_k | min_p | presence_penalty | repetition_penalty |
|---|---:|---:|---:|---:|---:|---:|
| 思考 | 1.0 | 0.95 | 20 | 0.0 | 0.0 | 1.0 |
| 非思考 | 0.7 | 0.80 | 20 | 0.0 | 1.5 | 1.0 |

推荐按本地模型路径识别，不按可自定义的服务别名判断，也不套用于其他模型。选择推荐并套用后保存生效；不会自动切换会话思考开关或覆盖输出预算。新增存在惩罚字段，支持显式 0，并验证传入实际请求。恢复服务默认会清空显式覆盖，继续继承服务配置。

- 源码：`064749c52fedef580eb1a07aa788da805720add5`。
- 独立候选：`local/vllm-sm75:v0.1.5-ultra-recommendation-review`。
- 镜像 ID：`sha256:e7500fbdb343afc2704978772e27f12e6bd7a7771df430f86b9ec4f5837210f8`。
- 基座：完整验收 ultra `sha256:54b5f40e12618348300d838a772cab569db355bc59a3ad68e223dd691d64eaa2`。
- Linux 58/58 通过；Windows 56 通过、2 个平台测试跳过。
- 独立容器 HTTP 验证通过：读取推荐、保存存在惩罚 1.5、显式 0、自定义值、恢复继承。
- 浏览器组件预览验证两套推荐回填正确，切回思考推荐时存在惩罚恢复为 0。

本次增量构建仅更新控制台；推理源码和构建配方未变。没有重跑 GPU 长上下文验收，之前 22 项结果仍归属于原完整镜像。未停止生产模型、未重启 Docker 服务、未部署生产或推送 GitHub，等待用户确认。
