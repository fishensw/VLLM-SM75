# RC7 正式页头显示修复

> 脱敏说明：部署地址、容器名及宿主路径已替换为示例占位；验证结论与哈希保持原值。

2026-09-18 已部署到 http://192.0.2.53:1615/。源码 13c86cb；镜像 local/vllm-sm75:v0.1.5-ultra-ui-rc7，ID sha256:bf9be0a77d47a4473f1ece7baa597b93dc65aa0d109ae5acd587af2c7c3e6348；源清单 SHA256 7a6211bba7509682cd0c15d96fca64dfba8d5098f9d4e62fb91229d760a85ba0。

用户报告刷新后未看到新页头。检查正式服务的 DSH 带版本资源，确认 RC6 资源确已生效；登录真实浏览器后发现两处问题：运行配置（默认首页）和模型库没有启用共享指标，窄视口下指标又被 flex 压缩成独立的小滚动框。RC7 将所有控制台页头统一接入共享指标，并保留指标固有宽度，窄视口由整条页头横向滚动。

## 验证

- 以 RC6 为父镜像，限制 1 CPU、1 GiB 内存、无网络构建；Linux 测试 52/52 通过。
- 用户在正式服务完成登录后，直接检查实际 DSH 页面，而非模拟预览。
- 1900×1000 桌面视口：运行配置、模型库、快速会话和工作台均显示 GPU、功耗、核心、显存、P、D、KV、命中。运行配置页头 clientWidth 与 scrollWidth 均为 1584，无横向裁切。
- 快速会话与工作台 header 的实际矩形均为 x=298、y=18、width=1584、height=53；指标内宽与滚动宽均为 957。
- 默认窄视口：指标内宽与滚动宽相等，整条页头容器承担溢出，不再嵌套挤压指标。完成后恢复浏览器默认视口。
- 运行配置切性能监控后 auth=authenticated、登录表单隐藏；真实浏览器未记录 JS error。延迟认证阶段证据见 RC6 记录。
- 模型健康 200，原会话 Cookie 保留，greedy 重复/自然停止、JSON Schema、工具调用往返通过。短输入缓存冒烟 cached tokens=0，不声称正命中证明。
- 部署脚本成功退出并输出 RC7_DEPLOYED_AND_VERIFIED。Docker daemon PID 26579 保持不变。仅替换应用容器，原配置、数据、密钥、模型与缓存挂载保留。
- RC6 停止保留为 vllm-sm75-ultra-rollback-rc6-20260918；Unraid 模板更新到 RC7。服务器记录目录 /path/to/sm75-artifacts/ui-rc7-20260918/。

页头按现有 5 秒周期采样，新面板初次加载可能等待一个采样周期。本次是 UI 增量部署，没有重跑长上下文测试，先前完整构建及长上下文记录保持独立。未推送 GitHub 或公开镜像。